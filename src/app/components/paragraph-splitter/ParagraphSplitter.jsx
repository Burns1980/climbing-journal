import React from 'react';
import PropTypes from 'prop-types';

// Takes text and splits it into paragraphs. Each paragraph is wrapped in a <p> tag and returned as an array of JSX <p> elements.
const ParagraphSplitter = ({ text }) => {
  return <div>ParagraphSplitter</div>;
};

export default ParagraphSplitter;
